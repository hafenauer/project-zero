name: Deploy and Run on Raspberry Pi

on:
  push:
    branches:
      - main
      - master

jobs:
  update-and-run:
    runs-on: self-hosted

    steps:
      - name: Pull latest code and restart service
        run: |
          cd ~/project-zero
          git pull
          sudo systemctl restart project-zero.service
